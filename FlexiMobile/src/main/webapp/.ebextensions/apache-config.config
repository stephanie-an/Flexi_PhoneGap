container_commands:
    01_setup_mod_deflate:
        command: "cp -u enable_mod_deflate.conf /etc/httpd/conf.d"
    02_setup_mod_expires:
        command: "cp -u enable_mod_expires.conf /etc/httpd/conf.d"
